export type RestaurantModel = {
  _id: string;
  name: string;
  rating: number[];
  minOrder: number;
  delivery: number;
  freeDelivery: number;
  description: string;
  cardImageUrl: string;
  pageImageUrl: string;
};
